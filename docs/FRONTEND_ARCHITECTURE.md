# REDFORGE AI — Frontend Architecture

- Vite + React 18 + TS + Tailwind + shadcn/ui
- React Router, Zustand, TanStack Query, WebSocket hook
- React Flow for attack graph
- Layout: sidebar (SOC dark), top bar, main canvas, right drawer for evidence/approvals
- Auth: JWT in httpOnly cookie fallback + localStorage, refresh flow
