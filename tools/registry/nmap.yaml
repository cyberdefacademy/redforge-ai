tool:
  name: nmap
  executable: /usr/bin/nmap
  category: reconnaissance
  risk_level: low
  timeout: 300
  requires_approval: false
  input_schema:
    type: object
    properties:
      target: {type: string}
      ports: {type: string}
