apiVersion: v1
kind: Secret
metadata:
  name: redforge-secrets
type: Opaque
stringData:
  # Fill via sealed-secrets / vault in real clusters. Never commit real values.
  db-url: "postgresql+asyncpg://redforge:CHANGE_ME@postgres:5432/redforge"
  jwt-secret: "CHANGE_ME_TO_32_PLUS_RANDOM_CHARS_MIN"
  seed-admin-password: "CHANGE_ME"
